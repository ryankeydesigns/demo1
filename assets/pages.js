const mobileType=document.createElement('style');
mobileType.textContent='html[lang="ja"] h1,html[lang="ja"] h2,html[lang="ja"] h3,html[lang="ja"] p,html[lang="zh-CN"] h1,html[lang="zh-CN"] h2,html[lang="zh-CN"] h3,html[lang="zh-CN"] p{line-break:strict;word-break:normal;overflow-wrap:break-word}h1,h2,h3,.lead,.copy,.card p,.event p,.center p,.source-note,.stat small,footer span{text-wrap:balance}.hero-inner{max-width:min(980px,72vw)}@media(max-width:900px){.nav nav a,.eyebrow,.section-no,.card small,.center a,.field label,footer{font-size:10.8px}.card p,.center p,.event p{font-size:13.2px}.source-note{font-size:12px}.copy,.lead{font-size:15.6px}.stat small{font-size:10.8px}.hero-inner{max-width:100%}}.page-hero:before{content:none!important}.subpage-dna{position:absolute;z-index:1;inset:76px 0 0 42%;width:58%;height:calc(100% - 76px);pointer-events:none}.page-hero .hero-inner{z-index:2}@media(max-width:900px){.subpage-dna{inset:76px 0 0 0;width:100%;height:calc(100% - 76px);opacity:.42}}';
document.head.appendChild(mobileType);
const i18nScript=document.createElement('script');i18nScript.src='assets/i18n.js';document.head.appendChild(i18nScript);
document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('.nav nav')?.classList.toggle('open'));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
document.querySelector('#contact-form')?.addEventListener('submit',e=>{e.preventDefault();e.currentTarget.reset();document.querySelector('.notice').classList.add('show')});

const hero=document.querySelector('.page-hero');
if(hero){
  const canvas=document.createElement('canvas');canvas.className='subpage-dna';canvas.setAttribute('aria-hidden','true');hero.prepend(canvas);
  const ctx=canvas.getContext('2d'),page=location.pathname.split('/').pop()?.replace('.html','')||'about';let tick=0,w=0,h=0;
  function fit(){const d=Math.min(devicePixelRatio||1,2),r=canvas.getBoundingClientRect();w=r.width;h=r.height;canvas.width=w*d;canvas.height=h*d;ctx.setTransform(d,0,0,d,0,0)}
  function dot(x,y,r=1.5,red=false,a=.7){ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fillStyle=red?`rgba(215,45,56,${a})`:`rgba(235,237,235,${a})`;ctx.fill()}
  function helix(scan=false){const cx=w*.56,span=Math.min(h*.38,250);for(let i=0;i<58;i++){const p=i/57,y=h*.5-span+p*span*2,ang=p*Math.PI*5+tick*.012,rad=(.35+.65*Math.sin(p*Math.PI))*Math.min(w*.28,190),x1=cx+Math.sin(ang)*rad,x2=cx-Math.sin(ang)*rad;ctx.beginPath();ctx.moveTo(x1,y);ctx.lineTo(x2,y);ctx.strokeStyle=i%7?'rgba(120,125,127,.14)':'rgba(215,45,56,.36)';ctx.stroke();dot(x1,y,i%8?1.1:2,false,.58);dot(x2,y,i%5?1.1:2,i%4===0,.66)}if(scan){const y=(tick*1.4)%(h+80)-40;ctx.fillStyle='rgba(215,45,56,.22)';ctx.fillRect(w*.18,y,w*.72,1);ctx.fillStyle='rgba(215,45,56,.035)';ctx.fillRect(w*.18,y-20,w*.72,40)}}
  function virus(){const cx=w*.59,cy=h*.5,R=Math.min(w,h)*.24;ctx.strokeStyle='rgba(110,115,117,.22)';ctx.lineWidth=1;ctx.beginPath();ctx.arc(cx,cy,R,0,Math.PI*2);ctx.stroke();for(let i=0;i<42;i++){const a=i/42*Math.PI*2+tick*.004,r=R*(.55+.16*Math.sin(i*2.1+tick*.015)),x=cx+Math.cos(a)*r,y=cy+Math.sin(a)*r;dot(x,y,i%7?1.3:2.4,i%6===0,.72);if(i%3===0){const x2=cx+Math.cos(a)*(R+18),y2=cy+Math.sin(a)*(R+18);ctx.beginPath();ctx.moveTo(cx+Math.cos(a)*R,cy+Math.sin(a)*R);ctx.lineTo(x2,y2);ctx.strokeStyle='rgba(215,45,56,.22)';ctx.stroke();dot(x2,y2,1.7,true,.55)}}}
  function network(){const nodes=Array.from({length:28},(_,i)=>{const a=i*2.399+tick*.0015,r=Math.sqrt(i/28)*Math.min(w,h)*.36;return{x:w*.58+Math.cos(a)*r,y:h*.5+Math.sin(a)*r}});ctx.strokeStyle='rgba(120,125,127,.14)';for(let i=0;i<nodes.length;i++)for(let j=i+1;j<nodes.length;j++){const dx=nodes[i].x-nodes[j].x,dy=nodes[i].y-nodes[j].y;if(dx*dx+dy*dy<9000){ctx.beginPath();ctx.moveTo(nodes[i].x,nodes[i].y);ctx.lineTo(nodes[j].x,nodes[j].y);ctx.stroke()}}nodes.forEach((n,i)=>dot(n.x,n.y,i%8?1.4:2.8,i%7===0,.72))}
  function report(){const count=34,bw=w*.55/count,left=w*.3;for(let i=0;i<count;i++){const wave=(Math.sin(i*.55+tick*.025)+1)/2,bh=35+wave*h*.42,x=left+i*bw,y=h*.5-bh/2;ctx.fillStyle=i%7===0?'rgba(215,45,56,.48)':'rgba(105,110,112,.28)';ctx.fillRect(x,y,Math.max(2,bw*.48),bh);dot(x+bw*.24,h*.5+Math.sin(i*.35+tick*.014)*h*.2,1.2,i%7===0,.6)}}
  function signal(){const cx=w*.59,cy=h*.5;for(let r=35;r<Math.min(w,h)*.42;r+=34){ctx.beginPath();ctx.arc(cx,cy,r+Math.sin(tick*.025+r)*4,0,Math.PI*2);ctx.strokeStyle=r%68?'rgba(110,115,117,.13)':'rgba(215,45,56,.24)';ctx.stroke()}for(let i=0;i<24;i++){const a=i/24*Math.PI*2+tick*.006,r=55+(i%6)*28;dot(cx+Math.cos(a)*r,cy+Math.sin(a)*r,i%5?1.2:2.4,i%6===0,.7)}}
  function draw(){ctx.clearRect(0,0,w,h);ctx.globalCompositeOperation='lighter';if(page==='about')helix();else if(page==='research')helix(true);else if(page==='virus')virus();else if(page==='centers')network();else if(page==='reports')report();else signal();ctx.globalCompositeOperation='source-over';tick++;requestAnimationFrame(draw)}
  fit();draw();addEventListener('resize',fit);
}
